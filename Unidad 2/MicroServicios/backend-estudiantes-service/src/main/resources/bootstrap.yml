spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: backend-estudiantes-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: TingesoEstudiantes
    url: jdbc:postgresql://localhost:5432/TingesoEstudiantes
    username: postgres
    password: 1234
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB